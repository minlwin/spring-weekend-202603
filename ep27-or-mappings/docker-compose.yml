volumes:
  db_data:
    driver: local

services:
  db:
    image: mysql:9.6
    container_name: mysql_db
    environment:
      - MYSQL_DATABASE=mydb
      - MYSQL_ROOT_PASSWORD=secret
      - MYSQL_USER=myuser
      - MYSQL_PASSWORD=mypassword
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql
