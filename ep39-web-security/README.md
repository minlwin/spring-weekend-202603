# Getting Start

After cloning repository, we have to install node modules.
```
npm i
```

Create Environment Config file. (.env)

```
BASE_API=http://localhost:8080
```

Run Application
```
npm run dev
```

You have to start docker containers for database and mail server and backend application.

You can check with http://localhost:3000